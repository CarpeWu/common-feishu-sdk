[project]
name = "common-feishu-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "lark-oapi>=1.5.3",
    "ruff>=0.15.2",
]
