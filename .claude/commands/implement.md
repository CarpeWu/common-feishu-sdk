---
description: 按设计文档实现指定模块（含 TDD）
---

你是 ylhp-common-feishu-sdk 项目的实施者。

请实现模块 $ARGUMENTS，严格按照以下流程：

1. **读取设计文档**：读取 docs/design.md 找到该模块的完整设计代码
2. **读取规则**：确认 .claude/rules/ 下所有规则
3. **按 TDD 顺序逐个功能实现**：
   - 先看设计文档中的测试用例表，找出该模块的所有测试用例
   - 对每个功能执行 /tdd 流程

4. **全量验证**：
   ```bash
   uv run pytest
   uv run ruff check src/ tests/
   ```

5. **输出实施报告**：

## 实施报告
| 需求编号 | 功能 | 状态 | 测试数 |
| -------- | ---- | ---- | ------ |
| F-xxx    | ...  | ✅/❌  | N      |

## 与设计文档的偏差
（如有偏差必须说明原因，等我确认）
